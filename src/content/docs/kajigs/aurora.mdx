---
title: Aurora
description: Aurora is a tool similar to Ventoy for ChromeOS RMA shims and recovery images based on Alpine Linux
author: soap phia
keywords: ["tool", "ventoy", "any kernver", "aurora"]
sidebar:
    order: 3
kernver: 0
---

{frontmatter.description}<br/>
{frontmatter.author && <small><i>By {frontmatter.author}</i><br/></small>}

#### What are the features?
- Recovery
- Payloads
- Booting other shims
- Wifi connectivity

## Prebuilts
Prebuilts are located at https://dl.mrwork.shop/aurora
## Building
### Dependencies
Arch Linux:
```
sudo pacman -Sy wget curl gptfdisk rsync binwalk e2fsprogs
yay -S vboot-utils
```
Debian:
```
sudo apt install wget curl bash e2fsprogs gdisk cgpt rsync vboot-utils
```
Alpine:
```
sudo apk add curl wget bash e2fsprogs gptfdisk sgdisk cgpt rsync vboot-utils
```
### Creating the image
```
git clone https://github.com/AerialiteLabs/Aurora.git
cd Aurora
```
Run the next command with a raw shim or nanoshim
```
sudo bash Aurora /path/to/shim.bin
```
Alternatively, you can automatically download a shim and build it with the following command:
```
sudo bash Aurora <board> --auto
```

## Flashing
Linux: (Replace /dev/sdX with your flash drive/sd card)
```
sudo dd if=/path/to/board-aurora.bin of=/dev/sdX bs=1M status=progress
```
Windows: Download Rufus, select your USB, select board-aurora.bin

macOS: Download BalenaEtcher, select your USB, select board-aurora.bin

ChromeOS: Download [Chromebook Recovery Utility](https://chromewebstore.google.com/detail/chromebook-recovery-utili/pocpnlppkickgojjlmhdmidojbmbodfm), select "Use local image" in the settings menu of it, select your USB, select board-aurora.bin

## Booting
1. Press [Esc+Ref+Pwr], then [Ctrl+D] to enter developer mode.
2. Plug in your flash drive that you flashed {frontmatter.title} to.
3. Press [Esc+Ref+Pwr] again and allow the shim to boot up.

#### Upload files via another device
1. Use AFT on page 2 of Aurora (requires wifi connectivity)
2. Use a Linux computer with **ROOT ACCESS**
    - Mount your flash drive
        ```
        sudo mount /dev/sdX4 /mnt
        ```
    - Copy your files
        ```
        sudo cp /path/to/[recoveryimage|shim].bin /mnt/usr/share/aurora/images/[recovery|shims]/
        ```
:::note
ChromeOS' Files app will NOT WORK! Use VT2 or Crostini USB passthrough if you must.
:::